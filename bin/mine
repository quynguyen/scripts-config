#!/bin/bash
#Generated by JetBrains Toolbox 1.26.5.13419 at 2022-11-04T16:57:20.389110

declare -a ideargs=()
declare -- wait=""

for o in "$@"; do
  if [[ "$o" = "--wait" || "$o" = "-w" ]]; then
    wait="-W"
    o="--wait"
  fi
  if [[ "$o" =~ " " ]]; then
    ideargs+=("\"$o\"")
  else
    ideargs+=("$o")
  fi
done

open -na "/Users/quynguyen/Library/Application Support/JetBrains/Toolbox/apps/RubyMine/ch-0/222.4345.14/RubyMine.app/Contents/MacOS/rubymine" $wait --args "${ideargs[@]}"