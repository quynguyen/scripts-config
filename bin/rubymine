#!/bin/bash
#Generated by JetBrains Toolbox 1.27.2.13801 at 2022-12-30T08:45:11.186753

declare -a ideargs=()
declare -- wait=""

for o in "$@"; do
  if [[ "$o" = "--wait" || "$o" = "-w" ]]; then
    wait="-W"
    o="--wait"
  fi
  if [[ "$o" =~ " " ]]; then
    ideargs+=("\"$o\"")
  else
    ideargs+=("$o")
  fi
done

open -na "/Users/quynguyen/Library/Application Support/JetBrains/Toolbox/apps/RubyMine/ch-0/223.7571.183/RubyMine.app/Contents/MacOS/rubymine" $wait --args "${ideargs[@]}"